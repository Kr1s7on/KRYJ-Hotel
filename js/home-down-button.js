// Function to scroll down when called
function scrollDown() {
    // Get the element with the id 'down'
    const element = document.getElementById('down');
    // Scroll the element into view smoothly
    element.scrollIntoView({behavior: "smooth"});
}